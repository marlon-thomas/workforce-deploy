# Care Angels Workforce — sign-in provisioning blueprint (deployment-spec §B2/B5).
# install.sh renders this with the deployment's client credentials and hostname, and
# mounts it into authentik's blueprint directory; authentik applies it natively on
# startup (idempotent — safe on every boot).
version: 1
metadata:
  name: Care Angels Workforce application
entries:
  - model: authentik_blueprints.metaapplyblueprint
    attrs:
      identifiers:
        name: Default - Authentication flow
      required: false
  - model: authentik_blueprints.metaapplyblueprint
    attrs:
      identifiers:
        name: Default - Invalidation flow
      required: false
  - attrs:
      name: Workforce
      authorization_flow: !Find [authentik_flows.flow, [slug, default-provider-authorization-implicit-consent]]
      invalidation_flow: !Find [authentik_flows.flow, [slug, default-provider-invalidation-flow]]
      client_id: ${OIDC_CLIENT_ID}
      client_secret: ${OIDC_CLIENT_SECRET}
      sub_mode: user_email
      access_token_validity: hours=1
      refresh_token_validity: days=30
      redirect_uris:
        - matching_mode: strict
          url: ${WF_REDIRECT_URI}
      property_mappings:
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-openid]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-email]]
        - !Find [authentik_providers_oauth2.scopemapping, [managed, goauthentik.io/providers/oauth2/scope-profile]]
    identifiers:
      name: Workforce
    state: present
    model: authentik_providers_oauth2.oauth2provider
    id: workforce-provider
  - attrs:
      name: Workforce
      slug: workforce
      provider: !KeyOf workforce-provider
    identifiers:
      slug: workforce
    state: present
    model: authentik_core.application
  - attrs:
      username: admin
      name: Administrator
      type: internal
      is_active: true
      email: ${ACME_EMAIL}
      # Set only when the user is first created (idempotent without being
      # revert-on-restart: a later org password change must survive restarts).
      password: ${AK_ADMIN_PASSWORD}
    identifiers:
      username: admin
    conditions:
      - !Condition needs_admin_user
    state: present
    model: authentik_core.user
